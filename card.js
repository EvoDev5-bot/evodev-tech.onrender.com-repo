function alertUser() {
    alert("Hey, this is just a practice for design and layout, so the button you just clicked does not do anything.");
}
function goTo(link) {
    window.open("https://evodev-tech.onrender.com/" + link + ".html")
}